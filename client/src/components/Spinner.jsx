// Spinner.js
import React from 'react';
import styles from './Spinner.module.css'; // Create this CSS module

const Spinner = () => (
  <div className={styles.spinner}></div>
);

export default Spinner;
